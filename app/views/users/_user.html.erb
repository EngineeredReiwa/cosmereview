<div class="row d-flex flex-row align-items-center justify-content-center">
    <img class="col-sm-4 mx-1 rounded" src="<%= gravatar_url(@user, { size: 100 }) %>" alt="">
    <div class="col-sm-4 mx-1">
      <h3><%= @user.name %></h3>
    </div>
</div>
<div class="border-bottom" style="padding:10px;"></div> <!--下ボーダー -->
<br>